using SelectAndLoop;

enum Complexity
{
    Easy,
    Normal,
    hard
}

internal class EnumDemo
{
    void UseNums()
    {
        Complexity complexity = Complexity.Easy;
        if (complexity == Complexity.Normal)
        {

        }
    }
}